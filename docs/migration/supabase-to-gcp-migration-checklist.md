# Supabase → GCP Migration Checklist (Obsolete)

**Status:** Obsolete — the migration is complete; the platform runs on GCP (Cloud SQL,
Identity Platform, Cloud Run) and is off Supabase.

This checklist tracked the one-time migration from Supabase to GCP. That migration is done,
and the platform later moved to event sourcing (CUR-1170). Original content removed; see git
history.
