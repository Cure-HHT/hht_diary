# PHASE 4.3 TASK 3 — Spec additions for nine new REQ-d topics

## REQs claimed

| New REQ     | Topic mnemonic            | Title                                                   | Assertions | Hash     |
| ----------- | ------------------------- | ------------------------------------------------------- | ---------- | -------- |
| REQ-d00126  | REQ-SYNCPOLICY-INJECTABLE | SyncPolicy Injectable Value Object                      | A..C       | 8ab70c79 |
| REQ-d00127  | REQ-SKIPMISSING           | markFinal and appendAttempt Tolerate Missing FIFO Row   | A..C       | bee6ba5e |
| REQ-d00128  | REQ-BATCH                 | FIFO Batch Shape and Fill Cursor                        | A..H       | cb119791 |
| REQ-d00129  | REQ-DYNDEST               | Dynamic Destination Lifecycle                           | A..I       | d0843f75 |
| REQ-d00130  | REQ-REPLAY                | Historical Replay on Past startDate                     | A..C       | 254b541a |
| REQ-d00131  | REQ-UNJAM                 | Unjam Destination Operation                             | A..E       | 467a0d85 |
| REQ-d00132  | REQ-REHAB                 | Rehabilitate Exhausted FIFO Row                         | A..D       | 978d782e |
| REQ-d00133  | REQ-ENTRY                 | EntryService.record Contract                            | A..I       | f132a4cf |
| REQ-d00134  | REQ-BOOTSTRAP             | bootstrapAppendOnlyDatastore Contract                   | A..D       | 9ead5ddd |

Numbers claimed by inspection — `query_nodes(kind=requirement, level=DEV)` returned REQ-d00125 as the highest existing id; REQ-d00126..REQ-d00134 taken as the next nine. Hashes populated by `elspais fix`.

## Discovery queries

- `discover_requirements("destination lifecycle schedule batch", scope_id=REQ-p01001)` → 0 results. The dev-level work Phase 4.3 introduces has no existing descendants of REQ-p01001 that match.
- `discover_requirements("event service atomic transaction no-op", scope_id=REQ-p01001)` → 0 results. `EntryService.record` is new dev-level material; no pre-existing assertion in the graph covers this surface.
- `search("FIFO")` → REQ-d00119, REQ-d00124, REQ-d00117, REQ-d00122, REQ-d00123, REQ-p01001 — all already cited in the plan.

No new edges claimed to existing REQs in this task; the new REQs' `Implements:` lines list the applicable PRDs (`REQ-p01001`, `REQ-p00004`, `REQ-p00006`, as appropriate), and the edges materialize when elspais re-parses the spec files.

## Files touched

- `spec/dev-event-sourcing-mobile.md` — nine new top-level sections appended after REQ-d00125, each with `## Rationale`, `## Assertions`, and the `*End* *Title* | **Hash**: <8 hex>` footer.
- `spec/INDEX.md` — nine rows inserted under the DEV block; file is auto-regenerated by `elspais fix` (carries the `<!-- Auto-generated -->` banner).

## Assertion-text fidelity

Assertion A..X text for each new REQ matches the text specified in `PLAN_PHASE4.3_library.md` verbatim, with the following explicit resolutions:

- **REQ-ENTRY-D** — written in the revised form per design §6.8 (local-only transaction; FIFO fan-out deferred to `fillBatch`). The PLAN_PHASE4.3 text marks this assertion "revised per design §6.8" and supplies the revised wording.
- **REQ-BOOTSTRAP-C** — written with the clarification that the registry remains open to runtime `addDestination` calls per REQ-d00129-A (consistent with the plan's parenthetical "Registry is no longer boot-frozen").

## Checks

`elspais checks` → 31/31 passed; 310 requirements validated.
