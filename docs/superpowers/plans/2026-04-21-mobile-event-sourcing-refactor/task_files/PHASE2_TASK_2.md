# Phase 2 Task 2: Spec additions (REQ-d00117, REQ-d00118, REQ-d00119)

**Date:** 2026-04-21
**Owner:** Claude (Opus 4.7) on user direction
**Status:** COMPLETE

## Applicable existing assertions (from `elspais search`)

Prior-art REQs discovered by `elspais search`:

- `elspais search "storage backend transaction"` → no direct prior-art for the mobile-side storage contract; closest conceptual parents are REQ-p00004 (Immutable Audit Trail via Event Sourcing) and REQ-p01001 (Offline Event Queue with Automatic Synchronization).
- `elspais search "event schema entry_type"` / `"server_timestamp"` → no prior-art for the mobile event record shape; parents are REQ-p00004 and REQ-p00013 (Complete Data Change History), the same parents as the Phase-1 provenance REQ.
- `elspais search "FIFO destination queue"` → no prior-art; REQ-p01001 is the parent for per-destination FIFO semantics (which refines the offline queue into strictly-ordered per-destination streams).

APPLICABLE_ASSERTIONS for test-marker citation purposes: `REQ-p00004-A, REQ-p00004-B, REQ-p00013-A, REQ-p01001-A, REQ-p01001-D, REQ-p01001-K` (the parent assertions that REQ-d00117/118/119 refine).

## REQ numbers claimed

`elspais search` confirmed REQ-d00116 was the highest existing dev-level REQ. Next slots used:

| Plan nickname | Assigned | Title |
| --- | --- | --- |
| REQ-SB | REQ-d00117 | StorageBackend Transaction Contract |
| REQ-ES | REQ-d00118 | Event Record Schema |
| REQ-FIFO | REQ-d00119 | Per-Destination FIFO Queue Semantics |

All nickname references in `PLAN_PHASE2_storage_backend.md` substituted via `sed -i` (28 occurrences replaced, 0 remaining). Nickname references in `PLAN_PHASE3/4/5` and in `task_files/` are unaffected — those files already contained no nickname references.

## Parent links chosen

- **REQ-d00117** → `implements` REQ-p00004 (Immutable Audit Trail) AND REQ-p01001 (Offline Event Queue). The transaction contract is how atomic append-only writes are realized (REQ-p00004) and how per-destination queuing stays consistent (REQ-p01001).
- **REQ-d00118** → `implements` REQ-p00004 AND REQ-p00013 (Complete Data Change History). The shape of the event record is how the audit-trail obligation is physically met; same parents as REQ-d00115.
- **REQ-d00119** → `implements` REQ-p01001. Strict per-destination ordering and send-log retention are the mobile-side refinements of the PRD's offline-queue obligation.

## Assertion counts

- REQ-d00117: 6 assertions (A–F). Covers `transaction()` atomicity, `Txn` scoping, `appendEvent` co-atomicity with sequence counter, `upsertEntry` whole-row replace, `enqueueFifo` initial state, `backend_state` store name.
- REQ-d00118: 4 assertions (A–D). `entry_type` first-class, `server_timestamp` removed, top-level fields duplicate `metadata.provenance[0].*`, `aggregate_id` shape during the dual-path window.
- REQ-d00119: 4 assertions (A–D). One FIFO store per destination, nine required entry fields, three legal `final_status` values, retention-forever rule.

Phase 4 will append further assertions to REQ-d00119 for backoff, attempt accumulation, and `SyncPolicy` constants (labels E+ under the stable-labels rule).

## Spec file state

`spec/dev-event-sourcing-mobile.md` grew from 73 to 149 lines (+76). Hashes computed by `elspais fix`:

- REQ-d00117 hash: `edab4770`
- REQ-d00118 hash: `565ca059`
- REQ-d00119 hash: `27595d15`

`spec/INDEX.md` regenerated by `elspais fix`; three new rows appended in the DEVELOPMENT table at lines 313-315, keeping numeric sort order.

## Incidental changes reverted

`elspais fix` (full pass) flagged 5 unrelated PRD files (REQ-p00043, REQ-p70001, REQ-p01073, REQ-p01065, REQ-p00044) for "missing changelog section" additions and actually applied changes to two (`prd-questionnaire-session.md`, `prd-questionnaire-system.md`). Reverted via `git checkout --` on both to keep Phase 2 commits focused on CUR-1154 scope. Same rationale as Phase 1 Task 2.

## Checks status

`elspais checks` post-change: 31/33 passed, 2 warnings, 16 skipped — identical to pre-change baseline. No regressions.

## README placeholder state

`docs/superpowers/plans/2026-04-21-mobile-event-sourcing-refactor/README.md` still carries `REQ-d?????` placeholders for all phases (including Phase 1, which also didn't update the README). Following precedent, leaving those placeholders alone — the authoritative mapping lives in each phase's TASK_FILE and in the spec file. Keeps the Phase 2 diff minimal.

## Task complete

Ready for Task 3 (storage value types). No blockers.
