Read `CLAUDE.md` (project instructions) and `AGENT_DESIGN_PRINCIPLES.md` first.

For requirements, read `spec/README.md` and `spec/INDEX.md`, and query the requirement graph
via the elspais MCP (`discover_requirements` / `search` / `get_requirement`) rather than
grepping `spec/` directly.
