#!/usr/bin/env bash
# SKILL: add-checklist
# Add requirement implementation checklist to a Linear ticket

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PLUGIN_DIR="$(dirname "$SCRIPT_DIR")"

# Pass all arguments to the script
exec node "$PLUGIN_DIR/scripts/add-checklist.js" "$@"
