#!/usr/bin/env bash
# SKILL: enhance-links
# Enhance ticket descriptions with requirement cross-references

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PLUGIN_DIR="$(dirname "$SCRIPT_DIR")"

# Pass all arguments to the script
exec node "$PLUGIN_DIR/scripts/enhance-links.js" "$@"
