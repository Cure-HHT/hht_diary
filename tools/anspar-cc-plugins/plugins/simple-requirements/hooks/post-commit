#!/bin/bash
# =====================================================
# Post-Commit Hook Wrapper
# =====================================================
#
# Calls the change detection hook
#
# =====================================================

# Get the directory where this script lives
HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute the change detection hook
"${HOOK_DIR}/post-commit-change-detection"
