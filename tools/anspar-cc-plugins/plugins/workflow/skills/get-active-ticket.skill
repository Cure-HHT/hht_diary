#!/bin/bash
# Get active ticket ID (non-interactive)
# Usage: get-active-ticket.skill

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

"$SCRIPT_DIR/scripts/get-active-ticket.sh"
