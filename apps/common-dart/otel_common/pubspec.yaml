name: otel_common
description: Shared OpenTelemetry instrumentation for HHT Diary platform servers
version: 0.0.1
publish_to: none

# IMPLEMENTS REQUIREMENTS:
#   REQ-o00047: Performance Monitoring — OpenTelemetry integration
#   REQ-o00045: Error tracking

environment:
  sdk: ^3.10.7

dependencies:
  dartastic_opentelemetry: ^1.0.0-alpha
  shelf: ^1.4.2
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.15
  mocktail: ^1.0.4
