name: diary_shared_model
description: "Shared cross-wire event entry-types + payload schemas + canonical projections for the diary/portal event-sourcing surface (frozen 2026-05-29). Actions stay per-app."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  event_sourcing:
    git:
      url: git@github.com:Cure-HHT/event_sourcing.git
      ref: 9e04c17
      path: event_sourcing

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
