# IMPLEMENTS REQUIREMENTS:
#   REQ-d00102: Display full sponsor branding
#
# Shared server-side utilities for diary and portal functions.

name: shared_functions
description: "Shared server-side utilities for diary and portal functions"
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  shelf: ^1.4.2

dev_dependencies:
  test: ^1.25.15
  mocktail: ^1.0.4
  lints: ^5.0.0
