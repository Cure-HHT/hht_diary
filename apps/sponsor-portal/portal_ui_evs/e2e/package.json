{
  "name": "portal-ui-evs-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright UI automation for portal_ui_evs (coordinator link-code issue).",
  "scripts": { "test": "playwright test", "install-browsers": "playwright install chromium" },
  "devDependencies": { "@playwright/test": "^1.48.0" }
}
