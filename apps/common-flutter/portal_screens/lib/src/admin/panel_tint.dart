import 'package:flutter/material.dart';

/// Soft ice/mint fill used by the admin dialogs' emphasized panels
/// (Figma: the User Details identity card and the Assigned Sites
/// checklist box). Lives in the consumer because the design system's
/// raw color tokens are intentionally not exported.
const Color kAdminPanelTint = Color(0xFFF4F9F8);
