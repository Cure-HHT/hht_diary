#!/bin/bash
# Generated by anspar-wf v0.2.1 — DO NOT EDIT
# Config hash: c2c650c022d3
# Generated: 2026-03-22T18:43:17Z

set -e

# Colors for output
RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

REPO_ROOT=$(git rev-parse --show-toplevel)

# Baked-in version defaults (overridden by .github/versions.env if present)
export GITLEAKS_VERSION="${GITLEAKS_VERSION:-8.30.0}"
export SQUAWK_VERSION="${SQUAWK_VERSION:-2.35.0}"
export ELSPAIS_VERSION="${ELSPAIS_VERSION:->=0.57.0}"
export MARKDOWNLINT_CLI_VERSION="${MARKDOWNLINT_CLI_VERSION:-0.46.0}"
export RUFF_VERSION="${RUFF_VERSION:-0.14.10}"

# Load project version overrides (takes precedence over baked defaults)
if [ -f "$REPO_ROOT/.github/versions.env" ]; then
    while IFS='=' read -r key value; do
        [[ "$key" =~ ^#.*$ ]] && continue
        [[ -z "$key" ]] && continue
        key=$(echo "$key" | xargs)
        value=$(echo "$value" | xargs)
        export "${key}=${value}"
    done < "$REPO_ROOT/.github/versions.env"
fi

# --- check: commit_msg_ticket ---
# Config: CUR (default: "CUR")
# Commit-msg: exits 1 if first line doesn't match [PREFIX-NNN]
# Implements: REQ-d00002-B
# ---

COMMIT_MSG_FILE="$1"
COMMIT_MSG=$(cat "$COMMIT_MSG_FILE")
FIRST_LINE=$(echo "$COMMIT_MSG" | head -n1)

# Skip for merge, revert, fixup/squash commits
if echo "$FIRST_LINE" | grep -qE "^Merge (branch|pull request|remote-tracking)"; then
    exit 0
fi
if echo "$FIRST_LINE" | grep -qE "^Revert "; then
    exit 0
fi
if echo "$FIRST_LINE" | grep -qE "^(fixup|squash)! "; then
    exit 0
fi

TICKET_PATTERN='^\[CUR-[0-9]+\]'

if ! echo "$FIRST_LINE" | grep -qE "$TICKET_PATTERN"; then
    echo ""
    echo -e "${RED}ERROR: Commit message must start with a ticket number!${NC}"
    echo ""
    echo "Expected format: [CUR-NNN] description"
    echo "  Example: [CUR-42] fix: Add validation for user input"
    echo ""
    echo "Your commit message:"
    echo -e "  ${YELLOW}$FIRST_LINE${NC}"
    echo ""
    echo "To bypass this check (NOT RECOMMENDED):"
    echo "  git commit --no-verify"
    echo ""
    exit 1
fi


# =====================================================
# Custom Hook Extension
# =====================================================
# Source project-specific custom hook if it exists

if [ -f ".githooks/custom-commit-msg" ]; then
    echo -e "${BLUE}Running custom commit-msg hook...${NC}"
    source ".githooks/custom-commit-msg"
fi


# =====================================================
# All Validations Passed
# =====================================================

exit 0
