#!/bin/bash
# Generated by anspar-wf v0.2.1 — DO NOT EDIT
# Config hash: c2c650c022d3
# Generated: 2026-03-22T18:43:17Z

set -e

# Colors for output
RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

REPO_ROOT=$(git rev-parse --show-toplevel)

# Baked-in version defaults (overridden by .github/versions.env if present)
export GITLEAKS_VERSION="${GITLEAKS_VERSION:-8.30.0}"
export SQUAWK_VERSION="${SQUAWK_VERSION:-2.35.0}"
export ELSPAIS_VERSION="${ELSPAIS_VERSION:->=0.57.0}"
export MARKDOWNLINT_CLI_VERSION="${MARKDOWNLINT_CLI_VERSION:-0.46.0}"
export RUFF_VERSION="${RUFF_VERSION:-0.14.10}"

# Load project version overrides (takes precedence over baked defaults)
if [ -f "$REPO_ROOT/.github/versions.env" ]; then
    while IFS='=' read -r key value; do
        [[ "$key" =~ ^#.*$ ]] && continue
        [[ -z "$key" ]] && continue
        key=$(echo "$key" | xargs)
        value=$(echo "$value" | xargs)
        export "${key}=${value}"
    done < "$REPO_ROOT/.github/versions.env"
fi

# --- check: branch_protection ---
# Config: main master (default: "main master")
# Pre-commit: exits 1 on protected branch
# Implements: REQ-d00002-B
# ---

CURRENT_BRANCH=$(git branch --show-current)

for PROTECTED in main master; do
    if [ "$CURRENT_BRANCH" = "$PROTECTED" ]; then
        echo -e "${RED}ERROR: Direct commits to '$CURRENT_BRANCH' branch are not allowed!${NC}"
        echo ""
        echo "This repository enforces a feature branch workflow."
        echo "All changes must go through pull requests."
        echo ""
        echo "To fix this:"
        echo "  1. Create a feature branch: git checkout -b feature/your-feature-name"
        echo "  2. Commit your changes to the feature branch"
        echo "  3. Push and create a pull request"
        echo ""
        exit 1
    fi
done

# --- check: ticket_branch_consistency ---
# Config: CUR (default: "CUR")
# Config:  (default: "")
# Pre-commit: warns on ticket-branch mismatch (non-blocking)
# Implements: REQ-d00002-B
# ---

if [ -n "" ] && [ -x "" ]; then
    ACTIVE_TICKET=$("" --format=id 2>/dev/null || echo "")

    if [ -n "$ACTIVE_TICKET" ]; then
        CURRENT_BRANCH=$(git branch --show-current 2>/dev/null || echo "")

        if [ -n "$CURRENT_BRANCH" ]; then
            BRANCH_TICKET=$(echo "$CURRENT_BRANCH" | grep -oE 'CUR-[0-9]+' | head -1 || echo "")

            if [ -n "$BRANCH_TICKET" ] && [ "$BRANCH_TICKET" != "$ACTIVE_TICKET" ]; then
                echo -e "${YELLOW}WARNING: Ticket-branch mismatch detected!${NC}"
                echo ""
                echo "   Active ticket: $ACTIVE_TICKET"
                echo "   Branch ticket: $BRANCH_TICKET"
                echo "   Branch name:   $CURRENT_BRANCH"
                echo ""
            fi
        fi
    fi
fi

# --- check: secret_scan_staged ---
# Config: (none)
# Pre-commit: exits 1 if secrets detected in staged files
# Implements: REQ-d00002-B
# ---

if command -v gitleaks &> /dev/null; then
    echo -e "${BLUE}Scanning staged files for secrets...${NC}"

    if ! gitleaks protect --staged --no-banner --redact 2>&1; then
        echo ""
        echo -e "${RED}Gitleaks detected secrets in staged files!${NC}"
        echo "Remove secrets before committing."
        echo ""
        echo "To bypass this check (NOT RECOMMENDED):"
        echo "  git commit --no-verify"
        exit 1
    fi

    echo -e "${GREEN}No secrets detected in staged files.${NC}"
    echo ""
else
    echo -e "${YELLOW}WARNING: gitleaks not installed - skipping secret scan${NC}"
    echo "  Install: https://github.com/gitleaks/gitleaks#installing"
    echo ""
fi

# --- check: markdownlint_staged ---
# Config:  (default: "")
# Pre-commit: exits 1 if staged markdown files fail lint
# Implements: REQ-d00002-B
# ---

MARKDOWN_FILES_CHANGED=$(git diff --cached --name-only --diff-filter=ACM | grep '\.md$' || true)

# Apply exclusion pattern if configured
if [ -n "" ] && [ -n "$MARKDOWN_FILES_CHANGED" ]; then
    MARKDOWN_FILES_CHANGED=$(echo "$MARKDOWN_FILES_CHANGED" | grep -v '' || true)
fi

if [ -n "$MARKDOWN_FILES_CHANGED" ]; then
    echo -e "${BLUE}Linting markdown files...${NC}"
    echo ""

    if command -v markdownlint &> /dev/null; then
        MDLINT_CONFIG=""
        if [ -f ".markdownlint.json" ]; then
            MDLINT_CONFIG="--config .markdownlint.json"
        fi

        if ! echo "$MARKDOWN_FILES_CHANGED" | xargs markdownlint $MDLINT_CONFIG 2>&1; then
            echo ""
            echo -e "${RED}Markdown linting failed!${NC}"
            echo "Fix the errors above before committing."
            echo ""
            echo "To bypass this check (NOT RECOMMENDED):"
            echo "  git commit --no-verify"
            exit 1
        fi

        echo -e "${GREEN}Markdown linting passed!${NC}"
        echo ""
    else
        echo -e "${YELLOW}WARNING: markdownlint not installed - skipping${NC}"
        echo "  Install: npm install -g markdownlint-cli"
        echo ""
    fi
fi


# =====================================================
# Custom Hook Extension
# =====================================================
# Source project-specific custom hook if it exists

if [ -f ".githooks/custom-pre-commit" ]; then
    echo -e "${BLUE}Running custom pre-commit hook...${NC}"
    source ".githooks/custom-pre-commit"
fi


# =====================================================
# All Validations Passed
# =====================================================

exit 0
