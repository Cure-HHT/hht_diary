# elspais configuration - Core Repository
# Generated by: elspais init (v0.112.9)
# 
# Tip: Create .elspais.local.toml alongside this file for developer-local
# overrides (e.g. associate paths). It is deep-merged and gitignored.

version = 4

# Project identity

[project]
namespace = "REQ"
name = "HHT Diary"

# Requirement ID format and type definitions

[id-patterns]
canonical = "{namespace}-{level.letter}{component}"
separators = ["-"]
prefix_optional = false

[id-patterns.aliases]
short = "{level.letter}{component}"

[id-patterns.component]
style = "numeric"
digits = 5
leading_zeros = true
# pattern = "[A-Z]{2}[0-9]{3}"  # For alphanumeric
# max_length = 32               # For named style

[id-patterns.assertions] # "uppercase" | "numeric" | "alphanumeric" | "numeric_1based"
label_style = "uppercase"
max_count = 26
# zero_pad = false              # Pad numeric labels
# multi_separator = "+"         # Multi-assertion (A+B+C)

[id-patterns.associated]
enabled = false
position = "after_prefix"
format = "uppercase"
length = 3
separator = "-"

# Requirement level definitions with hierarchy rules

[levels.prd]
rank = 1
letter = "p"
display_name = "Product"
implements = ["prd"]

[levels.ops]
rank = 2
letter = "o"
display_name = "Operations"
implements = ["ops", "prd"]

[levels.dev]
rank = 3
letter = "d"
display_name = "Development"
implements = ["dev", "ops", "prd"]

# File scanning configuration

[scanning]
skip = ["node_modules", ".git", "__pycache__", "*.pyc", ".venv", ".env"]

[scanning.spec] # Spec file scanning
directories = ["spec"]
file_patterns = ["*.md"]
skip_files = ["README.md", "requirements-format.md", "INDEX.md"]
skip_dirs = ["roadmap", "regulations"]
# index_file = "INDEX.md"  # Index file for ordering

[scanning.code] # Code file scanning
directories = ["src", "apps", "packages"]
file_patterns = []
skip_files = []
skip_dirs = []
# source_roots = []    # Import resolution roots

[scanning.test] # Test file scanning and reference detection
directories = ["apps/**/test", "apps/**/integration_test", "packages/**/test", "packages/**/integration_test", "tools/**/tests", "database/tests"]
file_patterns = ["*_test.dart", "test_*.dart", "test_*.py", "*_test.sql", "test_*.sql"]
skip_files = []
skip_dirs = []
enabled = true
prescan_command = ""
reference_keyword = "Verifies"
reference_patterns = []

[scanning.result] # Test result file scanning
directories = ["build-reports"]
file_patterns = ["**/TEST-*.xml", "pytest-results.json"]
skip_files = []
skip_dirs = []
run_meta_file = ""

[scanning.coverage] # Code coverage report scanning
directories = ["."]
file_patterns = []
skip_files = []
skip_dirs = []
# file_patterns = ["coverage.json", "**/lcov.info"]

[scanning.journey] # User journey file scanning
directories = ["spec/user-journeys"]
file_patterns = ["*.md"]
skip_files = []
skip_dirs = []

[scanning.docs] # Documentation file scanning
directories = ["docs"]
file_patterns = ["*.md"]
skip_files = []
skip_dirs = []

# Validation rules

[rules]
protected_branches = ["main", "master"]

[rules.hierarchy] # Global hierarchy settings
allow_circular = false
allow_structural_orphans = false
# cross_repo_implements = true  # Cross-repo implements
# allow_orphans = false         # Allow orphaned nodes

[rules.format] # Format enforcement rules
require_hash = true
require_rationale = false
require_assertions = true
require_status = true
allowed_statuses = ["Active", "Proposed", "Draft", "Deprecated", "Superseded"]
# content_rules = []            # Content validation rules

[rules.format.status_roles]
active = ["Active"]
provisional = ["Draft", "Proposed"]
aspirational = ["Roadmap"]
retired = ["Deprecated"]

[rules.coverage.implemented]
full_direct = "ok"
full_indirect = "info"
partial = "warning"
none = "error"
failing = "error"

[rules.coverage.tested]
full_direct = "ok"
full_indirect = "info"
partial = "warning"
none = "error"
failing = "error"

[rules.coverage.verified]
full_direct = "ok"
full_indirect = "info"
partial = "warning"
none = "warning"
failing = "error"

[rules.coverage.uat_coverage]
full_direct = "ok"
full_indirect = "info"
partial = "info"
none = "info"
failing = "error"

[rules.coverage.uat_verified]
full_direct = "ok"
full_indirect = "info"
partial = "info"
none = "info"
failing = "error"

# Changelog enforcement

[changelog]
hash_current = true
present = false
id_source = "gh"
date_format = "iso"
author_id_format = "email"
allowed_author_ids = "all"

[changelog.require]
reason = true
author_name = true
author_id = true
change_order = false

# Keyword extraction settings

[keywords]
min_length = 3

# Hash and validation settings

[validation]
hash_mode = "normalized-text"
allow_unresolved_cross_repo = true
# hash_algorithm = "sha256"     # Hash algorithm
# hash_length = 8               # Hash truncation length
# strict_hierarchy = false       # Strict hierarchy mode

# Defined terms: glossary, index, and health checks

[terms]
output_dir = "spec/_generated"
duplicate_severity = "error"
undefined_severity = "warning"
unmarked_severity = "warning"

# Output settings

[output]
formats = []
dir = ""

